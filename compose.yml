services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: uk_weather
      POSTGRES_USER: weather
      POSTGRES_PASSWORD: weather
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U weather -d uk_weather"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
